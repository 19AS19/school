//part.1
/*let sum = 0;
const number = prompt('num');
let temp = number;
while (temp>0) {
    let rem = temp % 10;
    sum += rem**number.length;
    temp = Math.floor(temp/10);
}
if (sum==number) {
    console.log(`${number} is armstrong`);
}
else {
    console.log(`${number} isnt armstrong`);
};
*/
//part.2
var input = prompt("strong num")